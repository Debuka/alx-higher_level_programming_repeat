#!/usr/bin/python3

def multiply_by_2(a_dictionary):
    multiplied_dictionary = a_dictionary.copy()
    idx_key = list(new_dir.keys())

    for i in idx_key:
        multiplied_dictionary[i] *= 2

    return (multiplied_dictionary)
